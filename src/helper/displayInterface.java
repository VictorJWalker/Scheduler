package helper;

import POJOs.Appointment;
/**This is a functional interface designed to output values in an appointment object. */
public interface displayInterface {
    String displayDetails(Appointment a);
}
